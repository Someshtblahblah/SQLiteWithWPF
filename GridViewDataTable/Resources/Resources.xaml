<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation">

    <!-- Brushes and Colors -->
    <SolidColorBrush x:Key="GridBackgroundBrush" Color="#393939" />
    <SolidColorBrush x:Key="GridHeaderBrush" Color="#414141" />
    <SolidColorBrush x:Key="GridRowBrush" Color="#454545" />
    <SolidColorBrush x:Key="GridSelectedRowBrush" Color="#5D5D5D" />
    <SolidColorBrush x:Key="GridBorderBrush" Color="#555555" />
    <SolidColorBrush x:Key="TextBrush" Color="#DFDFDF" />
    <SolidColorBrush x:Key="HeaderTextBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="AccentBlueBrush" Color="#4DC3FA" />
    <SolidColorBrush x:Key="ButtonHoverColor" Color="#575757" />

    <!-- RadGridView Style -->
    <Style TargetType="telerik:RadGridView" x:Key="DarkRadGridViewStyle">
        <Setter Property="Background" Value="{StaticResource GridBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GridBorderBrush}"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="RowHeight" Value="44"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="16"/>
    </Style>

    <!-- Header Style -->
    <Style TargetType="telerik:GridViewHeaderCell">
        <Setter Property="Background" Value="{StaticResource GridHeaderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{StaticResource GridBorderBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="8,0,8,0"/>
    </Style>

    <!-- Cell Style -->
    <Style TargetType="telerik:GridViewCell">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,0,8,0"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- Row Style: Only base and selected state (true to your screenshot) -->
    <Style TargetType="telerik:GridViewRow" x:Key="DarkGridViewRowStyle">
        <Setter Property="Background" Value="{StaticResource GridRowBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Style.Triggers>
            <!-- Selected row: lighter gray from screenshot -->
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource GridSelectedRowBrush}" />
                <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}" />
            </Trigger>
            <!-- Focused row: also use selected style for simplicity -->
            <Trigger Property="IsCurrent" Value="True">
                <Setter Property="Background" Value="{StaticResource GridSelectedRowBrush}" />
                <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Pager Style (bottom bar) -->
    <Style TargetType="telerik:RadDataPager">
        <Setter Property="Background" Value="{StaticResource GridBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- Button Style for toolbar/pager -->
    <Style TargetType="Button" x:Key="GridIconButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonHoverColor}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>